package com.indra.dronmanager.model;

/**
 * Enum que representa las posibles orientaciones de un dron en la matriz de
 * vuelo.
 */
public enum Orientacion {
    /** Norte (↑) */
    N,

    /** Sur (↓) */
    S,

    /** Este (→) */
    E,

    /** Oeste (←) */
    O
}
